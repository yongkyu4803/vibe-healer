interface:
  display_name: "API Key Guardian"
  short_description: "Check API key exposure risk."
  default_prompt: "Use $api-key-guardian to check whether this project exposes API keys before I push it."
policy:
  allow_implicit_invocation: true
